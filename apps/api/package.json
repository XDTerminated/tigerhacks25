{
  "name": "@tigerhacks25/api",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "install:python": "pip install -e .",
    "migrate": "python run_migration.py"
  }
}
