{
  "name": "tigerhacks25",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=@tigerhacks25/web",
    "dev:api": "turbo run dev --filter=@tigerhacks25/api",
    "build": "turbo run build",
    "build:web": "turbo run build --filter=@tigerhacks25/web",
    "lint": "turbo run lint",
    "start": "turbo run start",
    "start:api": "turbo run start --filter=@tigerhacks25/api"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
